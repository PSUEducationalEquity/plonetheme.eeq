[buildout]
extends = psu_addons.cfg
eggs +=
    ${theme:name}

sources-dir = src
auto-checkout =
    ${theme:name}

[theme]
package_path = ${buildout:directory}/src/${theme:name}

[client_base]
zcml +=
    ${theme:name}

[sources]
plonetheme.eeq = git git@github.com:PSUEducationalEquity/plonetheme.eeq.git
